{
  "name": "guitartap-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Web port of GuitarTap — headless DSP core + tests (Phase 2).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "sync-oracle": "./tooling/sync-oracle.sh"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
