# STATUS — What's Left

**This is the single source of truth for open work.** If it isn't listed under
"Open work" below, it's done or out of scope. Every other doc in `Development/`
is either a detail doc for one of these items, living reference, or history —
see the doc map at the bottom. Detail docs keep their own granular checkboxes;
this file just points at them.

_Last updated: 2026-07-16 (end of day: macOS testing done + amended; next = multi-tap plate, then iPad/iPhone/PC/Linux)._

## Open work

| # | Item | Status | Detail doc |
|---|---|---|---|
| 1 | **1.0.2 release** — in real-build testing | **Candidate: Swift `1.0.2-398` · Python `1.0.2-440` · web `112`** — build number = commit count, **verified unchanged after the 2026-07-16 amends** (that is the whole point of amending: Swift 398 is on TestFlight and must keep matching). Release notes cumulative; marketing-site banner, hosted manual + stable-named PDFs all in place. **macOS testing on all three is DONE and verified**: guitar, single-tap brace + plate, 3-tap brace, PDFs. Everything it found is fixed + user-verified (item 7, and §1/§2 in MATERIAL-MULTITAP-DISCREPANCIES) or triaged to items 4/8/9/10/11/12. Web release notes gained 5 entries for defects that shipped in 1.0.1 (verified by checking the code **at tag 1.0.1**, not by reading prose); **Python correctly needs none** — its Measurement Type row only arrived in 1.0.2, so its `—` bug never reached a user. **NEXT: (a) capture + verify a MULTI-TAP PLATE on all three — see item 4, it doubles as a free experiment; then (b) repeat the whole suite on iPad · iPhone · PC · Linux** (Python's new `QStandardPaths` WAV-folder paths are the genuinely untested part; iPad/iPhone may settle item 4's root cause outright). **Then ship:** App Store submit · web deploy · GitHub release · publish site. ⚠ **Swift's working tree holds an uncommitted `@parity` comment — see the note under this table; do NOT commit it with the release.** | [FILE-PATHS-AND-NAMES-SPEC.md](FILE-PATHS-AND-NAMES-SPEC.md) §9c |
| 2 | **Architectural-parity restructure** (view layer) | Needs its own **spec** before any code moves — write it and size it first. Lands the two parked test items (frozen-peak-recalc's selection half + `annotation-state` going 3-way). | [RESTRUCTURE-NOTES.md](RESTRUCTURE-NOTES.md) |
| 3 | **Theme — Light / Dark / System** | Blocked on THEME-SPEC § 8 decisions (light gradient + chrome hexes). The **systemBlue token** now on all three (progress bar, phase label, Peak readout) is the seam this work owns. | [THEME-SPEC.md](THEME-SPEC.md) |
| 4 | **Swift's audio buffer size** (leaks into ring-out, peak freqs, WAV length) | **NOT blocking 1.0.2** (user decision 2026-07-16 — pre-existing, not a regression; symptoms cosmetic-to-minor). **Root cause: Swift's live buffers are 4800 frames (0.1 s), not the 1024 it requests** — `installTap(bufferSize:)` is only a hint; macOS delivers 4800. Python/web genuinely get 1024. Proven by two 3-app *simultaneous* captures of the same UMIK-1 tap (audio bit-identical — xcorr 1.000000, lag 0), where **web↔python agree to <0.001 Hz** (the control) and Swift alone diverges. **Three symptoms, one cause:** (1) ring-out quantized — Swift exactly 0.100 s vs python 0.0853 (4×1024) / web 0.064 (3×1024); (2) sub-bin peak shifts — the gated FFT window starts at *(trigger − preRollSamples)* and the trigger is buffer-quantized (1024-grid fires at onset+151, 4800-grid at onset+3287 → 3136-sample window shift), so Air reads **97.3 on Swift vs 97.2** on python/web; (3) session WAV = exactly 34×4800. **Fixes (not interchangeable):** **A** re-chunk Swift's tap to a fixed 1024 grid → retires all three divergences (+ fixes 100 ms input latency) but keeps resolution coarse; **B** compute ring-out from raw samples → sub-ms + cadence-independent; **C** anchor the gated window to the detected onset *sample*. B/C share the principle (never let the buffer grid define a measurement); A just makes the grid uniform. Do **A first**. Cheap first step: log `buffer.frameLength`. ⚠ Playback **cannot** reproduce this (Swift's playback hardcodes 1024) — the existing shared ~0.085 s ring-out golden agrees across platforms only because playback pins both to 1024. Ruled out: calibration as a decay factor, physical variation, WAV format. **⭐ NARROWED 2026-07-16 by the first plate test — the divergence is the SESSION'S FIRST TAP, not the gated path in general.** Plate L/C/FLC peaks, three apps, same taps: **fC is IDENTICAL on all three** (117.331500 Hz, magnitudes within 0.0002 dB) and **fFLC agrees to ~0.0005 Hz** — but **fL alone** diverges (Swift 66.947420 Hz / −49.5903 dB vs **67.034660 / −50.5370 on BOTH** Python and web, bit-identical to each other). So Swift's later taps re-align to the onset *perfectly*; only the first one misses. Three independent cases now agree: (a) plate — L wrong, C+FLC right; (b) brace (item 9) — a **single-phase** measurement, i.e. L only, and Swift diverged there too; (c) guitar — **all three agree to 0.001 dB**, and guitar uses a fixed **65536-sample non-gated** window instead of the gated one. **Working hypothesis: at session start the pre-roll ring buffer isn't deep enough for Swift to re-align the onset when chunks arrive 4800 samples (100 ms) at a time; by the second tap it's full and alignment succeeds.** That turns fix **C** (anchor the gated window to the detected onset sample) from "somewhere in the gated path" into a specific question: **pre-roll depth at session start**. Also note the **sign flips** — Swift reads 0.95 dB *higher* on plate-L but 2.02 dB *lower* on brace-L: a **random per-tap window offset**, exactly what buffer quantization predicts, not a systematic bias. Impact on plate: 0.087 Hz on 67 Hz = 0.13% → ~0.26% on Young's modulus (E ∝ f²) — immaterial per the standing call, but Swift's plate numbers WILL differ slightly from the other two in the PDF. **Gore Target Thickness 2.80 (python/web) vs 2.81 (swift) — NOT a material difference** (user, 2026-07-16, accounting sense: "Just used to validate that things are correct in the algorithms"). The user attributed the split to the fL deviation and was right; **reproduced through Python's shipping `calculate_gore_target_thickness`** with every other input identical (Classical Top **f_vs=60** — the snapshot's `customPlateStiffness: 75` is inert unless the preset is Custom): python **2.802376** / swift **2.805110** / web **2.802373** mm. **The real disagreement is 2.7 MICRONS**; the rounding boundary is 2.805 and Swift lands 0.11 µm past it, so a sub-micron margin flips the displayed digit — the "0.01 mm" is a rounding artifact, not a disagreement. ⭐ **The value of this was VALIDATION, not a defect finding:** the thickness split is *exactly* what the fL deviation predicts through fL → E_L (∝ fL²) → thickness (∝ 1/√E_L), computed independently — and **python vs web agree to 3 NANOMETRES** (2.802376 vs 2.802373). Two independent implementations of the whole Gore chain landing that close is strong evidence **the algorithms are correct on all three**; the only variable is Swift's first-tap fL. ⚠ Do NOT "fix" the rounding or the thickness formula — both are correct and identical on all three; the defect is upstream in Swift's first-tap gated window. **⭐ TWO FREE EXPERIMENTS ARE COMING — read the results, don't just file them (planned 2026-07-16, before the multi-tap plate + device sweep):** **(1) The multi-tap plate FALSIFIES or CONFIRMS the first-tap hypothesis.** Single-tap plate gave Swift fL 66.947 vs **67.035** on both others (0.087 Hz). With a **3-tap plate the L phase averages three taps and only the FIRST is misaligned**, so two of three are clean → **if the hypothesis holds, Swift's fL gap should SHRINK markedly** (order of a third of 0.087 Hz) while **fC and fFLC still agree exactly** (their phases are never the session's first tap, whatever the tap count). **If the gap stays ~0.087 Hz the hypothesis is WRONG** and it is not merely the first tap. Costs nothing — just diff the three `.guitartap` files afterwards. **(2) iPad/iPhone may settle the root cause outright.** The claim is that **macOS** hands Swift 4800-frame buffers instead of the 1024 it requests; **iOS is a different audio stack**. If iOS delivers 1024 then **Swift on iPad should report fL 67.0347 — matching Python and the web exactly** — which nails the buffer beyond argument and makes fix **A** (re-chunk to a fixed 1024 grid) the obvious move. If Swift-on-iPad *still* diverges, the story is bigger than the buffer and **A alone will not be enough**. Cheap first step either way: log `buffer.frameLength` on both platforms. | [AUDIO-BUFFER-SIZE.md](AUDIO-BUFFER-SIZE.md) |
| 5 | **Audio watchdog is blind to a SILENT stream** | **NOT blocking 1.0.2** (self-recovers; needs a contending app; never yields a *wrong* measurement, only no measurement). Found 2026-07-16 in web testing with 3 apps sharing one UMIK-1. **The watchdog's "alive" test is `chunks are arriving`, not `chunks contain audio`** — `onChunk` stamps `lastChunkTime` on every chunk whatever it holds, so a stream delivering digital silence looks perfectly healthy and the watchdog never fires: **no recovery, no console output**. Two incidents: (a) mic level dead + mic-indicator on + Swift/Python fine + console empty → reload; (b) amplitude meter dormant while the peak indicator kept **changing below −100 dB** for minutes, then self-healed — chunks arriving, FFT running, on nothing (room floor is ≈ −70). Refuted along the way (don't re-chase): suspended AudioContext (Safari sat backgrounded for minutes still taking audio) and watchdog-gave-up (that path logs loudly; console was empty). **CONFIRMED on all three** (incident 3, same day): every app read ≈−100 dB at once because the UMIK-1 itself had wedged — **macOS System Settings → Sound → Input showed level 0**, yet CoreAudio still advertised it healthy + default, and **all three apps sat reporting −100 dB with no warning, no recovery, no log** (fixed by unplug/replug; suspected trigger: hours with three simultaneous clients). The silence was the hardware's; the **silent failure is ours** — and note even the OS's device-presence API wouldn't have helped, since the device *was* present: **the only available signal is that the samples are silent**, which is why the fix must be signal-level. **Fix needs design:** a signal-level criterion (sustained peak/RMS < ~−100 dBFS while armed = dead stream) carries real false-positive risk (muted mic / unplugged interface must not be "recovered" in a loop) → long dwell + consider surfacing "no audio input" rather than only auto-healing. | [AUDIO-WATCHDOG-SILENT-STREAM.md](AUDIO-WATCHDOG-SILENT-STREAM.md) |
| 6 | **Project hub repo — one home for project docs + cross-repo bug tracking** | 📋 IDEA, captured 2026-07-16 — **no action today**; needs a spec. Create a **new GitHub repo whose sole purpose is project documents + cross-repo information**, and use its **Issues as the single bug tracker**. **Two problems it solves.** (1) *The docs are split*: `Development/` (STATUS + every spec) lives in the **web** repo while `PARITY-MAP.md` / `parity-index.json` / `TEST-COVERAGE.md` + the parity tooling live in canonical **GuitarTap** — and the doc map below already calls `Development/` "transient" while saying living references belong in canonical GuitarTap. Neither location is right: these describe **all three** editions. (2) *Bugs are cross-repo*: the watchdog (item 5) is one bug in three codebases; the buffer-size one (item 4) is a Swift defect currently tracked in the web repo. **Enabler: the Swift code is going open source** → the public/private asymmetry disappears (GuitarTap is private today, the other two public), so the hub can be public, cross-repo issue refs resolve for everyone, and the community can see/file. **Tracking rules we settled on:** one bug = **one issue with platform labels** (`swift`/`python`/`web`/`all-three`) — *never* one issue per repo (three issues for one bug drift apart); commits in any repo reference it cross-repo (`refs owner/repo#N`); a **`needs-verification`** label encodes the standing rule that nothing is done until the USER runs it; meaty analyses stay as **detail docs in the repo** and the issue is the addressable index (the current STATUS-row → detail-doc pattern, with an ID a commit can point at — today our commit messages are essays precisely because there's nothing to reference). **Open questions for the spec:** where the parity **tooling** lives (it scans all three repos); whether generated artifacts (map / index / coverage) are committed there; migration + whether doc history is preserved; relative links between docs (move them together); whether per-repo Issues stay as community intake (`guitar_tap` already has a real 2024 public report) or redirect; naming. **Until it exists:** markdown STATUS continues, and items 4 + 5 migrate to Issues once the hub is up. | *(none yet — spec to be written)* |
| 7 | **Web PDF: material (brace + plate) report** — layout/styling/missing rows | ✅ **DONE + USER-VERIFIED 2026-07-16** — *"I just verified the plate and brace measurements pdfs. All look good."* A–L implemented; verified against the exported artifacts, not just the code. Web build exit 0 · 278 tests · Python 488 tests · parity `--check` exit 0. **§L measured: 3.50 MB → 0.18 MB** — the web's PDF is now SMALLER than Swift's (0.61) and Python's (0.62). ⚠ **A stale deploy nearly cost an afternoon**: a brace PDF exported *after* the fix but *by the old server build* looked un-reordered, while `npm run dev` was correct — **a PDF's timestamp says when it was exported, not which code exported it.** **✅ One follow-up fixed + verified after the first pass:** "Plate Properties" could be left as an **orphaned heading** at the foot of a page with its content overleaf (pagination itself is fine — already ruled so; this was the heading not travelling with its first block). Cause: the heading and the Sample Dimensions box each called `ensure()` separately, so the heading claimed the last 30pt and the box paginated away without it — Swift never hits this because its page auto-grows. Fixed by reserving both together via a new `threeColBoxHeight()`. Simulated across the 90pt danger zone: **46/90 positions orphaned before, 0/90 after.** 278 tests green; **user-verified in a re-exported PDF**. **G was WITHDRAWN — a phantom I invented** (I read "Young's Modulus (L):6.09 GPa" as missing a space off a *rasterised* image; measured with jsPDF, every label gets an identical 2.80 pt gap — don't re-chase). **K was also applied to Python** (user's call: *"if we do K for web we should do it for python"*) — needs Python's PDF run-reviewed too. **§F grew into a structural fix**: the quality-colour map was DUPLICATED and both copies had drifted — `Good` was **yellow** where Swift is `.blue`, `Very Good` was *Excellent's* green not `.mint`; both **hue** errors. Now ONE scheme-qualified table in the new `src/presentation/qualityColors.ts` (`light`/`dark`), verified against **Python's** documented system-colour hexes (`.green #34C759 · .mint #00C7BE · .blue #007AFF · .orange #FF9500 · .red #FF3B30`) — my first attempt guessed and got all five wrong. **This also fixes a Results-panel bug item 11 never saw** (its screenshot was cut off above Overall Quality). New parity slug **`model/quality-colors`** mirrors the `model/mode-colors` pattern. ⚠ **The Swift + Python `@parity` comments for that slug are UNCOMMITTED ON PURPOSE** — see the note under this table. **§H was worse than logged**: Swift keeps TWO frequency formatters deliberately (`formattedAsFrequency` `%.1f` for the metadata row, `formatFreq` `%.0f` for the chart's Range line) and the web had **each one's rounding on the other**. **§K: Swift's "border" is a MATTE, not a stroke** — `.background(Color(white:0.05)).cornerRadius(6)` showing through the chart PNG's transparent padding, which is why it reads "a little too thick" (it's SwiftUI's default padding). Web+Python now draw it deliberately (#0D0D0D, r6, 5pt). **Thinning it is a 3-platform change incl. Swift → post-release.** Original scope (user): *"If we can fix the discrepancies in the web PDF for brace and plate then that is enough for this release."* (user: *"If we can fix the discrepancies in the web PDF for brace and plate then that is enough for this release"*). Web-only; Swift canonical; every **value** is correct — this is placement, styling, and a few missing rows. **⭐ The brace bug IS in the plate report — it was camouflaged.** Same `twoColRows()` cause: Swift fills **column-major** (each property's L and C paired *vertically*), the web fills **row-major**, which *accidentally* puts every `(L)` in the left column and every `(C)` on the right and so reads like a deliberate L/C split. ⚠ **Don't judge the fix by whether the output "looks reasonable" — compare to Swift item by item.** **Web fix list:** (A) Sample Dimensions **2 columns, no box** vs Swift's **3 columns + grey box** — this is why thickness sits on a different line, **and it pushes `Density` onto page 2**, splitting the box across a page break; (B) properties row-major → column-major; (C) fL/fC/fLC row loses its grey box and its **bold values**; (D) **`GLC (Shear Modulus)` row missing** from the web's properties list (it's only in the Gore box); (E) Gore box sub-lines swapped (web puts GLC before Body/f_vs); (F) **Overall Quality "Good" is orange on web, blue** on Swift/Python; (G) missing space after the colon — `Young's Modulus (L):6.09 GPa`; (H) Frequency Range `20 Hz – 200 Hz` vs Swift `20.0 Hz – 200.0 Hz` — **same formatter as item 11 §2; fix once**; (I) chart subtitle `Detected Peaks: 3` vs `Comparing 3 measurements`; (J) legend `Series:` vs `Measurements:`; (K) chart border — **Swift is the odd one** (only Swift draws it), a decision not a fix. ⚠ Don't just flip `twoColRows`: its three callers need **different** treatment (dimensions=3 cols; props=column-major; ratios **unverified**) — **read Swift's plate branch `PDFReportGenerator.swift:~799-820` first** (8 props + ratios; its fill was inferred from the rendered PDF, not the source). **NOT web bugs, found while comparing → post-release:** (M) **Swift's plate chart chips show GUITAR MODE names** — `Unknown`/`Air (Helmholtz)` instead of the roles Python+web show correctly; "Air (Helmholtz)" is meaningless on a plate; (N) Swift's legend drops the `(L)`/`(C)` suffixes; (O) Python's date reads `Jul 16, 2026, 3:56 PM` vs `… at 3:56 PM` — check [[project_datetime_format_consistency]] first. **Confirmed NOT problems:** Detected Peaks order is correct on all three (the web's **PDF** sorts by frequency even though its **panel** doesn't — item 11 §1; don't "fix" the PDF to match the panel's bug); thickness present everywhere; page count content-dependent. | [WEB-PDF-MATERIAL-LAYOUT.md](WEB-PDF-MATERIAL-LAYOUT.md) |
| 8 | **Swift test case MISSING: `measurementTypeName` — Swift is `—` in the parity map** | 📋 OPEN, **created 2026-07-16, deliberately deferred, must not be lost.** Python + web now have tests for the Details-pane type resolution (parity group **`test/measurement-type-name`**, `PARITY-MAP.md:63`) — **Swift's slot is empty**, i.e. the **canonical** implementation of a rule the other two are tested against is itself untested. **Blocker is testability, not effort:** `measurementTypeName` is `private` inside the `MeasurementDetailView` struct (`MeasurementDetailView.swift:71`), and Swift `private` is unreachable from a test even with `@testable import` (that exposes only `internal`). So the test needs **dropping `private`** or **extracting the resolver** — an *application* change, which the 1.0.2 candidate on TestFlight must not take (user, 2026-07-16: Swift may be touched *only* where it does not change application code; the parity-map regen + docs were fine on that rule). **Do after the 1.0.2 ship.** ⚠ Consider folding into item 2 rather than a one-off: a private view-computed property that can't be tested is exactly the view-layer coupling that restructure targets. The bug this pins (Python's `—`) was **session-scoped** — invisible after any app restart, and *unreachable by round-trip tests* — so the test must build an **in-memory** measurement (snapshot-only, top-level field nil). | [MATERIAL-MULTITAP-DISCREPANCIES.md](MATERIAL-MULTITAP-DISCREPANCIES.md) §2 |
| 9 | **Material multi-tap: Swift's magnitude ~2 dB below Python/web** | 🔶 REAL, **NOT blocking 1.0.2** (user, 2026-07-16: *"the frequency value is what is important"* — fL agrees on all three at 513.0 Hz, and every derived property follows frequency + dimensions, not this level) — **but must be resolved after the release.** 3-tap brace: Swift −56.05 dB vs **−54.03 on both** Python and web, whose 16384-bin spectra are **bit-identical to each other** (`max|Δ| = 0.000000`) — two correct implementations on the same audio. **Averaging is PROVEN correct on all three and is NOT the suspect** (each guitar file's stored average == the power-domain mean of its own `tapEntries`, `max|Δ| = 0.0000`; log-domain mean is off by ~16 dB). Suspect is **item 4's 4800-frame buffer**: Swift's peak sits at 512.8569 Hz vs 512.8797 on both others — the same sub-bin shift signature — and its 100 ms chunks can't align the 400 ms gated window to the onset *sample* the way Python/web do. **⭐ The recorded session WAVs are the test bed** (user's point): replay one file through all three for a *deterministic* comparison instead of re-tapping — but first confirm `OUT-4-DETECTION-SPEC.md`'s finding still holds, that file playback exercises the same detection path as live. **Likely resolves together with item 4.** Guitar path is unaffected (all three agree to 0.001 dB). **⭐ STRENGTHENED 2026-07-16 by the first plate test — almost certainly the SAME defect as item 4, and the brace is simply its clearest case.** The plate shows the divergence is **the session's first tap only**: fC identical on all three, fFLC agreeing to ~0.0005 Hz, **fL alone** off on Swift. **A brace is a single-phase (L-only) measurement — so 100% of a brace measurement sits in the one phase that's broken**, which is why the brace looked like a whole-measurement 2 dB error while the plate looks like a single bad peak among three good ones. Same cause, different exposure. Also settles a loose end: Swift is 2.02 dB *lower* on brace-L but 0.95 dB *higher* on plate-L — the offset is **random per tap**, so no constant correction exists and "Swift reads low" was the wrong framing. **Resolve with item 4; do not chase separately.** ⭐ **The upcoming multi-tap plate is the experiment that tests this** — see item 4: if only the session's first tap misaligns, a 3-tap L phase should show a **markedly smaller** Swift fL gap than the single-tap plate's 0.087 Hz, with fC/fFLC still exact. A brace is the worst case precisely because it is L-only (100% of the measurement sits in the broken phase); a 3-tap **brace** should likewise improve, since 2 of its 3 taps align. | [MATERIAL-MULTITAP-DISCREPANCIES.md](MATERIAL-MULTITAP-DISCREPANCIES.md) §3 |
| 10 | **Web: row ⋯ menu clipped by the bottom of the window** | ⏳ **CODE WRITTEN, NOT VERIFIED** — fixed 2026-07-16, found by the user during 1.0.2 testing ("when the number of measurements gets large the menu on the ... is cut off by the bottom of the browser window"). **Web-only** (user decision): Swift/Python raise **native** menus that the OS places and keeps on-screen, so there is nothing canonical to mirror — a browser-only concern, tagged `@parity none` (precedent: `SpectrumViewGestureTests.swift`). **The menu already portaled to `<body>` with fixed positioning AND already had a flip-up** — but it was driven by a hardcoded guess at its own height: `openUp = menuRect.bottom > window.innerHeight - 170`, where the menu is **~262px** (7 items + 2 separators + padding). So rows with **170–262px of space below** — a ~92px **dead zone** — opened downward and were clipped, losing **Delete**. Only long lists put rows down there, which is exactly the reported trigger. **Fix = measure, don't guess:** `menuPlacement()` is now a **pure exported function** taking the button rect + the menu's real `offsetHeight` (measured in `useLayoutEffect`, which runs before paint → no flicker); it flips up only when the menu genuinely doesn't fit below *and* there's more room above, then clamps `maxHeight`/`overflowY` to the space available so it can never leave the viewport (no scrollbar when it fits). **A hardcoded constant goes stale every time a menu item is added — this cannot.** Tests: `test/menu-placement.test.ts` (14) — **verified to FAIL (6 failures) against the restored old rule**, so they catch the bug rather than describe it; the old rule is kept in the test file to guard the dead-zone cases against passing vacuously (one first-draft case did). Web build exit 0 · 278 tests · parity `--check` exit 0. **To run-review:** grow the library until rows sit near the window bottom, open ⋯ on the lowest rows — all items incl. Delete must be reachable; also try a short window. | *(this row)* |
| 11 | **Results panel (Analysis Results) — cross-platform consistency** | 📋 OPEN, captured 2026-07-16 from the first plate test. **Post-release; NOT a 1.0.2 blocker** — presentation only, every *number* is correct and agrees (the target-thickness check actually *validated* the algorithms). **Canonical = Swift.** ⚠ **NOT a "the web drifted" story — the divergences point THREE ways.** **(1) Peak row ORDER** *(code-confirmed)*: Swift + Python sort by ascending **frequency** (35.9, 66.9/67.0, 117.3); the **web** renders **phase order** L→C→FLC (67.0, 117.3, 35.9) — `MaterialResults.tsx:138-144` maps a `slots` array and never sorts, vs Swift `TapAnalysisResultsView.swift:410` `.sorted { $0.peak.frequency < $1.peak.frequency }`. ⚠ that slot array also drives the dashed placeholder rows during live capture, so a fix must keep slots while *capturing* and sort once complete. **(2) "Showing …" row** *(code-confirmed, THREE-way)*: Swift `Showing 20.0 Hz - 200.0 Hz` (hyphen, unit on both, 1 dp) · Python `Showing 20 – 200 Hz` (en-dash, unit once, 0 dp) · **web: the row is absent entirely** — and with it the Select All / Deselect All controls. **(3) Gore Target Thickness NESTING**: Swift + web put it first as its own section, then a separate Plate Properties; **Python** nests the Gore box *inside* a Plate Properties header. **(4) Gore BOXING**: boxed on Swift + Python, plain header on **web**. **(5) Label/line treatment** (user): "Speed of Sound" is its own **bold** line in Swift, but shares the line with the values and isn't bold on the **web** — audit ALL section labels, likely systemic. **(6) Colour for titles** (user): the **web** colours titles blue, Swift doesn't — ⚠ overlaps item 3 (THEME-SPEC systemBlue seam); settle it *with* the theme work or they'll fight. **(7) Vertical spacing** (user): **Python** is more cramped than Swift/web. **(8) ✅ ALREADY FIXED by item 7's §F (2026-07-16) — Overall Quality colour.** This panel renders `Good` from the same quality-colour map the PDF used, which had `Good` as **yellow** where Swift/Python show **blue** (and `Very Good` as Excellent's green rather than `.mint`). **This list never caught it because the web screenshot was cut off above Overall Quality** — a reminder that the "everything below Specific Modulus is unverified" warning is load-bearing. Now sourced from the single scheme-qualified table (`presentation/qualityColors.ts`, `dark` here / `light` for the PDF). Needs run-review with the rest of item 7. **⚠ Everything from Specific Modulus DOWN is unverified** — the three screenshots are cut off at different points; get scrolled-down captures before spec'ing. **Spec first against Swift** (the `MEASUREMENT-DETAILS-CONSISTENCY.md` pattern that worked), then all three together; consider folding into item 2 — divergence 1 is the web re-deriving what Swift's model owns, the recurring theme of this cycle. | [RESULTS-PANEL-CONSISTENCY.md](RESULTS-PANEL-CONSISTENCY.md) |
| 12 | **Swift + Python defects found during 1.0.2 testing (NON-web)** — the index, so they aren't lost in web docs | 📋 OPEN, **post-release** — all found 2026-07-16 while comparing the plate/brace reports. **This row exists because these were buried in web-scoped docs** (a Swift bug filed inside `WEB-PDF-MATERIAL-LAYOUT.md`, under an item marked "web-only, in scope for 1.0.2", is how a defect disappears — the exact complaint behind item 6). **Nothing here is a 1.0.2 blocker.** **(M) ⚠ SWIFT — plate chart chips show GUITAR MODE names.** The "Detected Peaks Summary" chips read `Unknown` / `Unknown` / **`Air (Helmholtz)`** on a *plate*; Python and web correctly show the roles `FLC` / `Longitudinal` / `Cross-grain`. **"Air (Helmholtz)" is meaningless for a material measurement** — a plate/brace is defined as L / Cross-grain / FLC and must never show a guitar mode (user, 2026-07-16: *"Yep Swift is very incorrect on the chip"*). Root cause: `ExportableSpectrumChart.swift:626` `let summaryModeMap = peakModes.isEmpty ? GuitarMode.classifyAll(peaks) : peakModes` — material peaks legitimately carry **no** modes, so `peakModes` is empty and it falls back to running the *guitar* classifier over plate peaks. ⚠ **The same fallback pattern is at `:169` and `:747` — check all three.** Irony: the very next block in that file already branches on material (`if !materialSpectra.isEmpty { "Measurements:" } else { "Guitar Modes:" }`); the chips just don't ask. **Surfaces in BOTH products** that render `ExportableSpectrumChart`: the PDF report **and** the Export Spectrum PNG. Swift-only; canonical but wrong — do NOT "align" Python/web to it. Detail: [WEB-PDF-MATERIAL-LAYOUT.md](WEB-PDF-MATERIAL-LAYOUT.md) §M. **(N) SWIFT — chart legend drops the role suffixes**: `Longitudinal` / `Cross-grain` vs Python+web `Longitudinal (L)` / `Cross-grain (C)`. Same chart-export path as M — fix together. §N. **(O) PYTHON — date format**: `Jul 16, 2026, 3:56 PM` vs Swift+web `Jul 16, 2026 at 3:56 PM`. ⚠ Check [[project_datetime_format_consistency]] (unified 2026-06-25) FIRST — this may be locale-medium doing the right thing per platform, not a bug. §O. **(P) PYTHON — Results panel vertically cramped** vs Swift/web (user); corroborated by Python being the only platform whose whole panel fits without scrolling. Lives with the rest of the panel work → item 11 / [RESULTS-PANEL-CONSISTENCY.md](RESULTS-PANEL-CONSISTENCY.md) §7. **(Q) PYTHON — Results panel nests Gore Target Thickness** inside a "Plate Properties" header; Swift+web have it first as its own section → item 11 §3. | [WEB-PDF-MATERIAL-LAYOUT.md](WEB-PDF-MATERIAL-LAYOUT.md) §M–O · [RESULTS-PANEL-CONSISTENCY.md](RESULTS-PANEL-CONSISTENCY.md) §3, §7 |
| 13 | **Web: SAVED multi-tap guitar PDF was missing its 2nd page** | ⏳ **CODE WRITTEN, NOT VERIFIED — fixed 2026-07-16, web-only.** User: the multi-tap guitar PDF on Swift/Python has an extra page (per-tap waveforms + peaks) the web lacked. **Not a missing feature — a WIRING GAP.** The web already had the whole machinery: `multiTapPdfData(m)` builds both pages (page 1 averaged, page 2 = per-tap comparison synthesized from `tapEntries` + an "Averaged" entry, mirroring Swift `generateMultiTapReport`), and `exportMultiTapPdfReport` renders them — even the doc-comment specified the contract *"Caller gates on `m.tapEntries.length > 1`."* But of the **two** export sites only one obeyed it: **`App.tsx` (live "Export PDF Report") already routed multi-tap correctly; `MeasurementsPanel.tsx` (the SAVED-measurements row menu) always called the single-page path.** So a multi-tap guitar exported live got both pages; the *same measurement exported from the saved list* got one — which is exactly the path the user reviews from. Fix: added the same `tapEntries.length > 1` gate to `MeasurementsPanel.exportPdf`, copying App.tsx's proven pattern. Guitar-only by construction (plate/brace never store `tapEntries` — see MATERIAL-MULTITAP §5). Build exit 0 · 278 tests. **⚠ No test catches this class** (a wiring gap between two call sites; both existing suites passed before and after) — **to run-review: export a SAVED 3-tap guitar measurement's PDF and confirm page 2.** ⭐ **Structural note for item 2:** two export sites with the same gate copy-pasted is how they diverged in the first place — a shared `exportMeasurementPdf(m, filename)` helper (holding the gate once) would make this un-recurrable. Deferred (touching App.tsx, which already works, is out of this fix's scope). | *(this row)* |

### ⚠ DELIBERATELY UNCOMMITTED: the Swift `@parity model/quality-colors` comment

**Decision 2026-07-16 (user): "the uncommitted Swift code… stays uncommitted for now."**

Item 7's §F added a new parity slug `model/quality-colors`, which needs a `@parity` comment anchored
in each platform. The web has its (`src/presentation/qualityColors.ts`). The natives' anchors are
**5 comment lines each**, sitting **uncommitted** in the working tree:

- `GuitarTap/GuitarTap/Models/MaterialProperties.swift`
- `guitar_tap/src/guitar_tap/models/material_properties.py` *(this one rides along — Python is being
  amended + rebuilt anyway)*

**Why not committed:** the build number is the **commit count**, and the release artifacts are built
from the committed state. Swift 398 is already on TestFlight; committing five comment lines that
change no behaviour would force a Swift rebuild + re-upload and restart the test cycle. Uncommitted
comments cost nothing — the lesson being that *"doesn't change application code"* is about
**behaviour**, but a rebuild is triggered by **any source edit**. Docs (PARITY-MAP.md,
parity-index.json, TEST-COVERAGE.md) are free; sources are not.

**When to land it:** with the **first post-release Swift change** — items 4, 12/M and 12/N all touch
Swift, so it rides along free. Until then `parity --check` passes locally because the working tree
has both the comments and the regenerated map.

⚠ **Do not commit `MaterialProperties.swift` with the 1.0.2 web deploy.** If it is committed by
accident, Swift's build number moves and the TestFlight build no longer matches the tag.

## Done (for reference)

Everything else is complete:

- **File paths & names audit → 1.0.2** ✅ ALL 8 STEPS COMMITTED 2026-07-15 (one commit per repo).
  Unified where files save, what they're named, the required-name rule, and the WAV-folder/sandbox story
  across Swift/Python/web. Highlights: Step 6 = settable **WAV Capture folder** (path-comparison + a
  launch-B reachability prompt); its run-review bundled in two independent fixes — the **disarmed-idle
  button-enablement** fix (New Tap no longer gets stuck; `@parity state/button-enablement`) and the
  **audio-clock ring-out** fix (the REG_G "flake" was a real load-sensitive product bug;
  `@parity dsp/decay`, [DECAY-AUDIO-CLOCK-FIX.md](DECAY-AUDIO-CLOCK-FIX.md)). Step 7 removed dead
  `ExportView` (no cross-platform naming inconsistency existed). Step 8 = doc-impact audit (4 stale
  "New Tap only when complete" wordings fixed) + 1.0.2 release notes (build Swift 398 · Python 440 ·
  web 112) + regenerated User Manual (1.0.2) + a 1.0.2 marketing-site banner. Suites Swift 372 ·
  Python 479 · web 248. The **release itself** (cross-platform test → ship) continues as open item #1.
  Spec: [FILE-PATHS-AND-NAMES-SPEC.md](FILE-PATHS-AND-NAMES-SPEC.md).
- **Re-analyze enablement (`canReanalyze`)** ✅ all 3, user run-reviewed 2026-07-13 (suites green:
  Swift 351 · Python 459 · Web 233). Re-analyze is offered for **any complete guitar measurement with a
  frozen spectrum**, never material. It is a **reset**, not a dirty-flag indicator: enabled when it *could*
  do something, not only when we can prove it *will* — the staleness sources are open-ended, and a
  wrongly-disabled button is a dead end where a wrongly-enabled one costs a pointless click. The old rule
  (`loadedMeasurementPeaks != nil`) was a stale-peaks proxy wrong in both directions: it disabled itself
  after one press (the one-shot), and never lit up for a live capture whose mode assignments had drifted
  across Peak Min moves. The predicate now lives on the analyzer and all 3 views bind to it — as a view
  boolean it had been untestable, which is why the wrong proxy survived.
- **Web in-app Release Notes** ✅ user-reviewed — Help gains a third entry; TSX like the rest of the web's
  help, since the web ships no PDF and has no Markdown pipeline. `@parity none` (the web is always current,
  so notes for the running build belong in the app; Swift ships via the App Store, Python via GitHub).
- **Task 3 / 6-TEST** — test-suite normalization across the 3 repos ✅ (Phases 1–6). 0 parity orphans;
  living coverage reference is now `GuitarTap/TEST-COVERAGE.md` (canonical repo).
- **Task 4 / Platform parity gaps** — OUT-1…OUT-5 ✅ all shipped + user run-reviewed (2026-07-13).
  OUT-4 uncovered two canonical bugs: the relative noise-floor detector had **never actually run in file
  playback on any platform** (`skipWarmup` pinned the floor to −100, collapsing the relative rule onto the
  absolute one; and the warm-up was wall-clock, so it expired before any audio arrived). OUT-5 was resolved
  by **deletion** — the branch was unreachable on all three.
- Phases 2–5 and all of Phase 6; the entire Task 1 / Task 2 comment-&-doc-parity review (all `@parity`
  slugs + map cleanup). Material Results phased display shipped 2026-07-08.

## Doc map — what each file in `Development/` is

**Index**
| Doc | Purpose |
|---|---|
| [STATUS.md](STATUS.md) | *(this file)* the only open-work list |
| [README.md](README.md) | repo overview, build, deploy |

**Open-work detail docs** (the open items above)
| Doc | Purpose |
|---|---|
| [RESTRUCTURE-NOTES.md](RESTRUCTURE-NOTES.md) | view-layer restructure — raw material; spec still to be written |
| [THEME-SPEC.md](THEME-SPEC.md) | Light/Dark/System theme spec — blocked on § 8 |
| [AUDIO-BUFFER-SIZE.md](AUDIO-BUFFER-SIZE.md) | Swift's live buffers are 4800 frames, not the 1024 it requests — one root cause behind three symptoms (ring-out 0.1 s quantization, sub-bin peak shifts / Air 97.3 vs 97.2, WAV = 34×4800). The 3-app identical-audio evidence + fixes A/B/C. Not blocking 1.0.2 — 0.1 Hz is immaterial to guitar making, but it matters for cross-platform accuracy |
| [AUDIO-WATCHDOG-SILENT-STREAM.md](AUDIO-WATCHDOG-SILENT-STREAM.md) | the buffer-delivery watchdog treats "chunks arriving" as "alive", so a stream of digital silence looks healthy — the app goes deaf with no recovery and no console output. Two web incidents + the hypotheses they refuted; likely all three platforms. Not blocking 1.0.2 |

**Reference** (living — consult, don't complete)
| Doc | Purpose |
|---|---|
| [INVENTORY.md](INVENTORY.md) | the spec extracted from canonical Swift — the contract the port is measured against |
| [PLAN.md](PLAN.md) | overall migration plan & parity bar |
| [DEV-DOC-STANDARD.md](DEV-DOC-STANDARD.md) | comment/doc conventions for the review |
| [WEB-UI-GUIDELINES.md](WEB-UI-GUIDELINES.md) | how the desktop/mobile UI translates to the web |
| `GuitarTap/TEST-COVERAGE.md` *(canonical repo, not here)* | **living cross-platform test-coverage reference** — the `@parity` system, matrix + equivalence, the change-all-three rule, how to run the tools, current gaps/platform-only. Lives with the parity tooling + `PARITY-MAP.md` in canonical `GuitarTap`, not in this transient Development/ dir. |
| [PARITY-MAP.md](PARITY-MAP.md) | pointer — the generated coverage matrix lives in the Swift repo |

**History** (done — audit trail, not a to-do list)
| Doc | Purpose |
|---|---|
| [PLATFORM-PARITY-GAPS.md](PLATFORM-PARITY-GAPS.md) | Task 4 — cross-platform behavior/UI gaps OUT-1…OUT-5 — ✅ ALL DONE, user run-reviewed 2026-07-13 |
| [OUT-4-DETECTION-SPEC.md](OUT-4-DETECTION-SPEC.md) | OUT-4 spec + the empirical work behind it (why the relative model never ran in playback; the noisy-fixture design) — ✅ done; `tooling/make-noisy-fixture.py` regenerates the fixture |
| [PHASE6-PARITY.md](PHASE6-PARITY.md) | Phase 6 parity backlog — ✅ COMPLETE (last item § 6-TEST done); coverage now lives in `GuitarTap/TEST-COVERAGE.md` |
| [6-TEST-NORMALIZATION.md](6-TEST-NORMALIZATION.md) | 6-TEST normalization — ✅ COMPLETE (Phases 1–6); project history, living reference is `GuitarTap/TEST-COVERAGE.md` |
| [LOG-FREQ-REMOVAL.md](LOG-FREQ-REMOVAL.md) | removed the dead log-frequency axis — ✅ done 2026-07-08 (B1) |
| [TAPTONEANALYZER-CONSOLIDATION.md](TAPTONEANALYZER-CONSOLIDATION.md) | Phase 6-3c consolidation spec — ✅ done 2026-07-12 (analyzer/device split); parity gaps it surfaced live in PLATFORM-PARITY-GAPS.md |
| [STATUS-STATE-MACHINE.md](STATUS-STATE-MACHINE.md) | Status state-machine alignment (silent warm-up + derived status) — ✅ done (Swift+Python; web already conformant); fixed OUT-1 at the root |
| [REVIEW-TRACKER.md](REVIEW-TRACKER.md) | Task 1/2 doc-review tracker (done) + the spun-out effort specs |
| [REVIEW-FINDINGS.md](REVIEW-FINDINGS.md) | behavioural gaps found during the doc review + fixes |
| [PHASE2-DSP-HARNESS.md](PHASE2-DSP-HARNESS.md) | headless DSP test plan — done |
| [PHASE3-UI.md](PHASE3-UI.md) | React UI + material-measurement build — done |
| [PHASE4-PERSISTENCE.md](PHASE4-PERSISTENCE.md) | `.guitartap` persistence — done |
| [MATERIAL-RESULTS-PHASED-DISPLAY.md](MATERIAL-RESULTS-PHASED-DISPLAY.md) | phased Analysis Results — done 2026-07-08 |
| [MEASUREMENT-DETAILS-CONSISTENCY.md](MEASUREMENT-DETAILS-CONSISTENCY.md) | Measurement Details pane — implemented all 3 |
| [DATE-TIME-FORMAT-CONSISTENCY.md](DATE-TIME-FORMAT-CONSISTENCY.md) | date/time display — implemented all 3 |
| [SAMPLE-RATE-PLAN.md](SAMPLE-RATE-PLAN.md) | capture sample-rate recording — ✅ complete all 3 (verified 2026-07-08); only an optional Swift mock-device unit test remains, non-blocking |